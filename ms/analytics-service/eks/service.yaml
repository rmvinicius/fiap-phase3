apiVersion: v1
kind: Service
metadata:
  name: analytics-service-svc
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005