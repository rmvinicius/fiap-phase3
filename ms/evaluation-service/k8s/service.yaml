apiVersion: v1
kind: Service
metadata:
  name: evaluation-service-svc
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004